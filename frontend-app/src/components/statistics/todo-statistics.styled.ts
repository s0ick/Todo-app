import styled from 'styled-components';

export const TodoStatisticsBlock = styled.div`
  display: flex;
  justify-content: flex-start;
  align-items: center;
  
  svg, g, path {
    transition: fill .2s ease-in;
  }
`;
